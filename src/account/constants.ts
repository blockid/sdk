export enum AccountStates {
  Created = "CREATED",
  Deploying = "DEPLOYING",
  Deployed = "DEPLOYED",
}

export enum AccountDeviceStates {
  Created = "CREATED",
  Deploying = "DEPLOYING",
  Deployed = "DEPLOYED",
}
