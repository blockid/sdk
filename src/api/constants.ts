export enum ApiConnectionStates {
  Opening = "OPENING",
  Opened = "OPENED",
  Closed = "CLOSED",
}

export enum ApiSessionStates {
  Verifying = "VERIFYING",
  Verified = "VERIFIED",
  Destroyed = "DESTROYED",
}
